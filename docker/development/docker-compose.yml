version: "3"
services:
  next-web:
    depends_on:
      - db
    build:
      context: ../../
      dockerfile: docker/development/Dockerfile
    # image: donhatnamdocker/next-web-development:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DOCKER_POSTGRES_PRISMA_URL="postgresql://postgres:postgres@db:5432/users"
    networks:
      - backend-network
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRED_DB=users
    # We don't have to expose this port because the containers communicate inside the docker network
    #ports:
    #  - '5432:5432'
    volumes:
      - postgres-db-volume:/var/lib/postgresql/data


volumes:
  db:
    driver: local